@using ArimaaAnalyzer.Maui.Models
@using ArimaaAnalyzer.Maui.Services
@using ArimaaAnalyzer.Maui.Services.Arimaa
@using Microsoft.AspNetCore.Components
@implements IDisposable

<div class="gtc-container">
    @if (!string.IsNullOrWhiteSpace(StatusMessage))
    {
        <div class="gtc-status @StatusMessageCssClass" role="status">@StatusMessage</div>
    }

    @if (Game?.CurrentNode is null)
    {
        <div class="gtc-empty">No active node</div>
    }
    else if (Game.CurrentNode.Children is { Count: > 0 } children)
    {
        @if (!string.IsNullOrWhiteSpace(Game.PendingMoveText))
        {
            <div class="gtc-pending" title="Pending moves that can be reset or committed">
                Pending: @Game.PendingMoveText
            </div>
        }
        @if (ActionMode == GameTreeActionMode.Copy)
        {
            <div class="gtc-copy-hint">Copy mode: click a variation to save its line.</div>
        }
        @foreach (var child in children)
        {
            var label = FormatChildLabel(Game.CurrentNode, child);
            var isCopy = ActionMode == GameTreeActionMode.Copy;
            <div class="gtc-row @(ShowDeleteButtons ? "gtc-row-with-actions" : null)">
                @if (ShowDeleteButtons)
                {
                    <div class="gtc-actions">
                        <button type="button"
                                class="gtc-delete-btn"
                                title="Delete variation: @label"
                                aria-label="Delete variation: @label"
                                @onclick="() => HandleDelete(child)"
                                @onclick:stopPropagation="true">
                            <span aria-hidden="true">×</span>
                        </button>
                    </div>
                }
                <button type="button"
                        class="gtc-item @(isCopy ? "gtc-item-copy" : null)"
                        title="@(isCopy ? $"Copy variation to Load game list: {label}" : $"Go to this variation: {label}")"
                        @onclick="() => HandleChildClickAsync(child)">
                    @label
                </button>
            </div>
        }
    }
    else
    {
        <div class="gtc-empty">No children</div>
        @if (!string.IsNullOrWhiteSpace(Game.PendingMoveText))
        {
            <div class="gtc-pending" title="Pending moves that can be reset or committed">
                Pending: @Game.PendingMoveText
            </div>
        }
    }
</div>

@code {
    /// <summary>Cascaded from MainLayout top-bar Edit mode (Delete) on the Arimaa page.</summary>
    [CascadingParameter(Name = "ShowGameTreeDeleteButtons")]
    private bool ShowDeleteButtons { get; set; }

    /// <summary>Cascaded from MainLayout top-bar Edit mode (None / Delete / Copy).</summary>
    [CascadingParameter(Name = "GameTreeActionMode")]
    private GameTreeActionMode ActionMode { get; set; }

    [Parameter]
    [EditorRequired]
    public ArimaaGameService Game { get; set; } = default!;

    /// <summary>Copy mode: save the selected variation line under the given name.</summary>
    [Parameter]
    public EventCallback<GameTurn> OnCopyChild { get; set; }

    /// <summary>Optional status line shown above the child list (success / error from parent).</summary>
    [Parameter]
    public string? StatusMessage { get; set; }

    [Parameter]
    public string? StatusMessageCssClass { get; set; }

    protected override void OnInitialized()
    {
        if (Game is not null)
        {
            Game.CurrentNodeChanged += OnCurrentNodeChanged;
            Game.StateChanged += OnStateChanged;
        }
    }

    private void OnCurrentNodeChanged()
    {
        InvokeAsync(StateHasChanged);
    }

    private void OnStateChanged()
    {
        InvokeAsync(StateHasChanged);
    }

    private void HandleDelete(GameTurn child)
    {
        if (Game is null || child is null) return;
        Game.RemoveChild(child);
    }

    private async Task HandleChildClickAsync(GameTurn child)
    {
        if (Game is null || child is null) return;

        if (ActionMode == GameTreeActionMode.Copy)
        {
            if (OnCopyChild.HasDelegate)
                await OnCopyChild.InvokeAsync(child);
            return;
        }

        Game.Load(child);
    }

    /// <summary>
    /// Label for a child variation. Stored Moves is preferred; when empty (HumanMove /
    /// side-sync nodes), derive notation or a clear fallback from parent→child AEI.
    /// </summary>
    private static string FormatChildLabel(GameTurn parent, GameTurn child)
    {
        var stored = string.Join(" ", child.Moves ?? Array.Empty<string>()).Trim();
        if (!string.IsNullOrWhiteSpace(stored))
            return stored;

        // Try legal Arimaa step notation from AEI transition
        try
        {
            var before = new GameState(parent.AEIstring);
            var after = new GameState(child.AEIstring);
            var (notation, aei) = CorrectMoveService.ComputeMoveSequence(before, after);
            if (!string.IsNullOrWhiteSpace(notation)
                && !string.Equals(notation, "error", StringComparison.Ordinal)
                && !string.Equals(aei, "error", StringComparison.Ordinal))
            {
                return notation;
            }
        }
        catch
        {
            /* fall through to descriptive fallbacks */
        }
        return $"setup ({child.Side} to move)";
    }

    public void Dispose()
    {
        if (Game is not null)
        {
            Game.CurrentNodeChanged -= OnCurrentNodeChanged;
            Game.StateChanged -= OnStateChanged;
        }
    }
}
